name: Test

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  verify:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Run Tests
      uses: lunarmodules/busted@v2.3.0
      with:
        args: spec/
